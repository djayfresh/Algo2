package com.fresh.tree;

import java.util.HashMap;
import java.util.Map;


public class HuffmanTree<T>
{
	BinaryNode<T> root;
	public HuffmanTree(Byte[] bytes)
	{
		constructTree(bytes);
	}
	
	private void constructTree(Byte[] bytes)
	{
		Map<Byte, Integer> total = new HashMap<Byte, Integer>();
		
		for(int i = 0; i < bytes.length; i++)
		{
			int count = 0;
			for(int c = 0; c < bytes.length; c++)
			{
				if(bytes[i] == bytes[c])
				{
					count++;
				}
			}
			int value = 0;
			if(total.get(bytes[i]) != null)
			{
				value = total.get(bytes[i])
			}
			total.put(bytes[i], count + );
		}
	}
}
